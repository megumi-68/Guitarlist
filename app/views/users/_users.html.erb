<% if users.any? %>
  <ul class="list-unstyled">
    <% users.each do |user| %>
      <li class="media">
      <% if user.image? %>
       <%= image_tag user.image.thumb50.url, :alt => 'ユーザー画像' %>
      <% else %>
       <%= image_tag "/assets/default.u", :alt => 'デフォルト画像',:size => '100x100' %>
      <% end %>
       <div class="media-body">
        <div>
        <%= link_to user.name, user_path(user) %>
        </div>
       </div>
     </li>
    <% end %>
  </ul>
 <%= paginate users %>
<% end %>